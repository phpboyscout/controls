# Controls Package

The `controls` package delivers an extensible, feature-rich lifecycle management framework for executing long-running applications and services. It provides standardized routines for startup, graceful shutdown, health-checks, metrics exposure, and dependency initialization.

## Topics
- [Components: Controls](components/controls.md)
